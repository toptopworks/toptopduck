{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "默认能力：核心 + 文件拾取对话框 + 日志 + 窗口状态持久化（#5 / #98 / #100）",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default", "log:default", "window-state:default"]
}
